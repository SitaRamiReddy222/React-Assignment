{
  "homepage": "http://SitaRamiReddy222.github.io/React-Assignment",
  "name": "assignment",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "cd client && npm run build",
    "client-install": "cd client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "server": "npm install && nodemon app.js",
    "client": "npm run client-install && npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client \""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
